import { In } from "./in";

export class InLocations {
  static query: In = 'query';
  static header: In = 'header';
  static path: In = 'path';
  static formData: In = 'formData';
  static body: In = 'body';
}