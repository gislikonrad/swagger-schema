// import { InternalValidationSettings, ValidationType } from "src/validation-settings";

// export function url(target: any, name: string) {
//   if(InternalValidationSettings.type === ValidationType.none) return;
//   let value = this[name];
// }